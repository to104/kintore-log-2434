# 筋トレ記録アプリ 使い方

自分専用の筋トレ記録アプリ（PWA＝ホーム画面に追加してアプリのように使えるWebページ）。
一度iPhoneに入れれば、**完全オフライン**で動作します。

## ファイル構成

| ファイル | 役割 |
|---------|------|
| index.html | アプリ本体（画面・機能すべて入り） |
| sw.js | オフライン対応の仕組み（Service Worker） |
| manifest.webmanifest | ホーム画面追加時のアプリ情報 |
| apple-touch-icon.png / icon-512.png | ホーム画面用アイコン |

## iPhoneに入れる手順（初回のみ）

1. このフォルダの5ファイルを **GitHub Pages**（無料のWebページ公開サービス）にアップロードして公開する
   - GitHubアカウント作成 → 新規リポジトリ作成 → 5ファイルをアップロード
   - リポジトリの Settings → Pages → Branch を main にして Save
   - 数分後に `https://ユーザー名.github.io/リポジトリ名/` で公開される
2. iPhoneの **Safari** でそのURLを開く
3. 共有ボタン（□に↑）→「**ホーム画面に追加**」
4. 以後はホーム画面のアイコンから起動。**通信は一切不要**

> URLを知っている人しかアクセスできませんが、公開URLにはなるため、
> リポジトリ名は推測されにくい名前にしておくとより安心です。

## 機能

- **記録**: 種目・重量・回数を選んで「セット完了」。種目を切り替えると前回の重量・回数を自動入力
- **タイマー**: 30秒刻みで調整できる休憩タイマー。セット完了後の自動スタート対応（設定でON/OFF）
  - 残り3秒からカウント音、終了時に選んだ音が鳴り画面がフラッシュ
  - カウント中は画面が消灯しないよう制御（画面を点けたままにしておくこと）
- **音**: 電子ビープ／チャイム／ベル／アラーム／メロディの5種類から選択、音量調整・試聴可
- **履歴**: トレ日数・最大重量・今月の総挙上量、種目別の重量推移グラフ
- **書き出し**: CSV共有（AirDropやメールでPCへ）→ ExcelやClaude Codeで自由に集計
- **バックアップ**: JSON形式で保存・復元

## 注意点

- 記録データはiPhoneのブラウザ内（ローカルストレージ）に保存されます。
  ホーム画面に追加して使えば消えにくいですが、**月1回程度のバックアップ推奨**
- タイマーの音は、画面を消したりアプリを閉じたりすると鳴りません（iPhoneの仕様）
